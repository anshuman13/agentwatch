{
  "name": "agentwatch",
  "version": "0.1.0",
  "private": true,
  "description": "Build tooling for the agentwatch dashboard. Not required to run the plugin.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
