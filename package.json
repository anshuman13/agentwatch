{
  "name": "agentwatch",
  "version": "0.2.0",
  "private": true,
  "description": "Build tooling for the agentwatch dashboard. Not required to run the plugin.",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/index.html public/index.html",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
